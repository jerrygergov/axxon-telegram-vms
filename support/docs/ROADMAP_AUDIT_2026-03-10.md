# Roadmap Audit - 2026-03-10

## Scope and method

- Reviewed `support/docs/IMPLEMENTATION_PLAN_NEXT.md` step by step against checked-in code, docs, and tests under `scripts/`, `axxon_telegram_vms/`, `support/docs/`, and `tests/`.
- Status meanings:
  - `done`: concrete implementation exists and repo evidence supports the step at the current MVP scope.
  - `partial`: meaningful implementation exists, but one or more named deliverables in the plan are still missing.
  - `not done`: no meaningful implementation beyond plan text or generic groundwork.
- Absence claims below were checked with repository search on 2026-03-10.

## Summary

- `done`: 10
- `partial`: 15
- `not done`: 5

## Step-by-step audit

1. `done` - `support/docs/RUNTIME_BASELINE.md:1-151` freezes the runtime baseline and explicitly lists commands, callbacks, runtime modules, tests, environment variables, and known pain points.
2. `not done` - there is no separate feasibility-matrix document under `support/docs/`; the only checked-in feasibility material remains the plan text itself in `support/docs/IMPLEMENTATION_PLAN_NEXT.md:66-85`, with no repo artifact mapping features to confidence, blockers, MVP scope, and deferred scope.
3. `done` - `support/docs/REGRESSION_CHECKLIST.md:1-48` provides the standing regression checklist, including required local checks and manual runtime checks.
4. `done` - `support/docs/ARCHITECTURE_FOUNDATION.md:9-46` defines the `client/services/models/jobs/ui` split, and `tests/test_architecture_foundation.py:18-52` locks the seeded lanes while keeping `scripts/` as the active runtime.
5. `partial` - `axxon_telegram_vms/client/__init__.py:3-37` exports config/session/transport/cache seams, but the runtime still calls the CLI bridge through subprocesses (`scripts/tg_ui_common.py:25-34`, `support/docs/RUNTIME_BASELINE.md:139-143`). The foundation doc still lists direct client extraction as pending (`support/docs/ARCHITECTURE_FOUNDATION.md:79-82`).
6. `partial` - the models lane currently exports `NormalizedDetectorRow`, `NormalizedEvent`, `EventQuery`, and request envelopes (`axxon_telegram_vms/models/__init__.py:3-74`), but there are no first-class `NormalizedCamera`, `NormalizedHost`, `NormalizedMacro`, `NormalizedAcfaUnit`, `NormalizedArchiveSegment`, or `NormalizedSearchResult` exports. The baseline still calls the normalized domain model partial (`support/docs/RUNTIME_BASELINE.md:142-143`).
7. `done` - the event normalization layer exists in `axxon_telegram_vms/models/event_normalization.py:273-597`, including paired/instant phase handling, alert trigger semantics, friendly labels, and detector/camera extraction. `tests/test_models_normalization.py:50-105` covers motion, LPR, and alert normalization behavior.
8. `partial` - caching is real but narrow. `axxon_telegram_vms/client/runtime_cache.py:12-145` only caches camera catalog, detector inventory, camera UI, server info/version/stats, and admin UI. The broader repo-wide cache called for in the plan is still explicitly pending (`support/docs/ARCHITECTURE_FOUNDATION.md:79-84`, `support/docs/RUNTIME_BASELINE.md:143`).
9. `partial` - job primitives exist for live sessions and future external analysis (`axxon_telegram_vms/jobs/live_sessions.py:12-89`, `axxon_telegram_vms/jobs/external_analysis.py:14-133`), but there is still no generic export/search job model with progress text and output artifacts for archive/export/search work. That gap is also called out in `support/docs/ARCHITECTURE_FOUNDATION.md:79-84`.
10. `partial` - the command surface has grown, but it is still flat rather than regrouped by the plan's operator/subscriptions/admin/utility buckets. `scripts/axxon_tg_bot.py:1329-1352` registers a flat menu, and `scripts/axxon_tg_ui.py:1275-1288` still renders a flat home button grid with no `/digest`, `/hosts`, `/macros`, `/acfa`, or `/profile` surface.
11. `partial` - there is an admin-only split, not a full operator/admin UI split. `scripts/axxon_tg_ui.py:1286-1288` adds an admin button only for admins, and `scripts/axxon_tg_bot.py:1580-1709` gates `/macro`, `/ptz`, and `/admin`. But the operator home/menu remains the same base layout for all users (`scripts/axxon_tg_ui.py:1233-1288`).
12. `not done` - secure profile storage exists, but it currently persists timezone preferences rather than favorites/pinned cameras (`scripts/axxon_tg_bot.py:852-875`). The command surface also has no favorites/profile workflow (`scripts/axxon_tg_bot.py:1329-1352`).
13. `partial` - normalized labels are used in event/alert buttons and latest-alert home buttons (`scripts/tg_camera_ui.py:24-51`, `scripts/axxon_tg_ui.py:1275-1278`), and label behavior is tested in `tests/test_models_normalization.py:50-105`. But repository search shows `label_primary` consumers only in UI button builders, not in subscription delivery paths, so the "subscription notification titles" part of the step is still missing.
14. `not done` - there is no digest command or digest delivery model in the runtime. The current subscription seam only exposes ledger/filter helpers (`scripts/subscription_services.py:1-22`), and the visible bot command list still stops at `/subscriptions`, `/stop`, and `/stopall` (`scripts/axxon_tg_bot.py:1329-1352`).
15. `not done` - repository search found no topic-routing implementation (`message_thread_id`, topic rules, or per-topic routing) in `scripts/axxon_tg_bot.py`, `scripts/subscription_services.py`, or `axxon_telegram_vms/services/`. The bot UI also exposes no topic or broadcast-digest controls (`scripts/axxon_tg_bot.py:1329-1352`, `scripts/axxon_tg_ui.py:1275-1288`).
16. `partial` - host/domain visibility exists inside the admin foundation: `axxon_telegram_vms/services/admin_view.py:180-246` shapes host/domain/platform summaries and `scripts/axxon_web_api.py:2286-2348` fetches `/hosts` plus `/hosts/{host}`. But there is still no dedicated `/hosts` or `/host <name>` Telegram command in `scripts/axxon_tg_bot.py:1329-1352`.
17. `partial` - macro inventory normalization and single-macro preview exist (`axxon_telegram_vms/services/macro_execution.py:160-454`), and `scripts/axxon_web_api.py:1869-1932` reads the macro inventory before preview/execute. But the exposed surfaces are only `macro-preview`, `macro-execute`, and `/macro`; there is no read-only list/search macros workflow (`scripts/axxon_web_api.py:1344-1365`, `scripts/axxon_tg_bot.py:1580-1629`).
18. `partial` - PTZ read-only preview is real: `axxon_telegram_vms/services/ptz_control.py:136-142, 419-604` supports inspect/goto-preset previews with capabilities, absolute position, and preset inventory. But tours are only surfaced as a capability flag (`tests/test_ptz_control_service.py:44-57`), and the flow is admin-only rather than a broad read-only visibility surface (`scripts/axxon_tg_bot.py:1632-1693`).
19. `not done` - there is no ACFA service, UI, or test coverage. The only non-plan ACFA hits are macro action-family constants in `axxon_telegram_vms/services/macro_execution.py:11-23, 25-42`, which is not ACFA inventory or read-only visibility.
20. `done` - the shared query/filter abstraction is implemented in `axxon_telegram_vms/models/query_filters.py:210-540`, covering time range, host/domain/camera/detector scope, taxonomy, and text/mask filters. `tests/test_query_filters.py:15-171` exercises normalization, matching, and round-trip behavior.
21. `done` - time-range search is implemented through `axxon_telegram_vms/services/event_search.py:32-444`, with summary/list modes, pagination, and host/camera/detector/category filters. `tests/test_event_search_service.py:79-190` covers parsing, backend shaping, summary, and pagination, and the bot exposes `/search` in `scripts/axxon_tg_bot.py:1335-1339`.
22. `partial` - archive jump/search MVP exists (`axxon_telegram_vms/services/archive_jump.py:159-597`, `scripts/axxon_tg_ui.py:553-620`) and returns preview/context-handle metadata, but it is still explicitly single-camera (`axxon_telegram_vms/services/archive_jump.py:128-129`) and the UI offers only back/home buttons, not adjacent archive navigation (`scripts/axxon_tg_ui.py:615-620`).
23. `partial` - the current export MVP is one bounded single-camera clip path. `axxon_telegram_vms/services/single_camera_export.py:59-257` supports one camera plus `mp4` export, and `/export` is wired in `scripts/axxon_tg_bot.py:1570-1577`. There is still no first-class frame export job or "around event" export preset in the service or command surface.
24. `partial` - the multi-camera model is present but planning-only. `axxon_telegram_vms/services/multi_camera_export.py:107-497` builds plans and aggregates results, while `scripts/axxon_web_api.py:1325-1334` exposes `multi-camera-export-plan`. There is no Telegram command or runtime batch execution surface in `scripts/axxon_tg_bot.py:1329-1352`.
25. `done` - license-plate search supports exact, contains, and mask matching with time-range and scope filters in `axxon_telegram_vms/services/license_plate_search.py:124-488`. `tests/test_license_plate_search_service.py:52-133` verifies parsing, backend request shaping, and pagination, and `/plate` is registered in `scripts/axxon_tg_bot.py:1336-1339`.
26. `partial` - the uploaded-image face-search MVP is implemented as a narrow JPEG + gRPC flow (`axxon_telegram_vms/services/face_search.py:523-735`) with capability detection and archive/export follow-up actions, and is covered by `tests/test_face_search_service.py:81-206`. But the step called for an upload -> search job -> results workflow, and the current implementation is still a direct synchronous request path with no dedicated job primitive for face searches.
27. `done` - macro execution ships with allowlists, admin gating, confirmation, audit logging, and visible result shaping. The guardrails live in `axxon_telegram_vms/services/macro_execution.py:137-454`, the Telegram confirm/cancel flow is in `scripts/axxon_tg_bot.py:1580-1629`, and execution logging happens in `scripts/axxon_web_api.py:1887-1932`. `tests/test_macro_execution_service.py:63-125` covers parsing, allow/deny rules, and result shaping.
28. `partial` - selective PTZ control exists, but only for preset jumps. The backend path acquires a PTZ session and calls `GoPreset` (`scripts/axxon_web_api.py:1992-2088`), and the service explicitly warns that point/area PTZ remains out of scope (`axxon_telegram_vms/services/ptz_control.py:477-480`). There is no tour-control or bounded step-move implementation.
29. `done` - the admin-view foundation is in place. `axxon_telegram_vms/services/admin_view.py:137-536` defines the admin policy, host/access summaries, and future write-plane boundaries; `scripts/axxon_web_api.py:2286-2348` populates the read-only admin payload; `tests/test_admin_view_service.py:120-166` covers the result shape.
30. `done` - Step 30's extension points are implemented as additive contracts. `support/docs/FUTURE_EXTENSION_FOUNDATION.md:1-64` describes the intended seams, `axxon_telegram_vms/services/external_analysis.py:1-229` plus `axxon_telegram_vms/jobs/external_analysis.py:14-133` provide the external-analysis abstraction, and `tests/test_external_analysis_service.py:14-98`, `tests/test_request_envelopes.py:6-31`, and `tests/test_session_contracts.py:11-63` cover the new contracts.
